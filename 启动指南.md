# 油田吊装作业安全视频智能分析系统（AegisLift）— 启动指南

> 本系统分前后端两部分，需各开一个终端启动。后端 FastAPI（端口 8000），前端 Vue（端口 5173）。

## 0. 环境要求

| 组件 | 版本 | 检查命令 |
|------|------|----------|
| Python | 3.10+（当前用 Miniconda 3.13） | `python --version` |
| Node.js | 16+（建议 18/20 LTS） | `node -v` |
| npm | 随 Node 安装 | `npm -v` |

---

## 1. 启动后端（终端 1）

```bash
cd D:\University-Competition\Innovation_Entrepreneurship\project\backend-python

# 首次运行先装依赖（已装过可跳过）
pip install -r requirements.txt

# 启动（二选一）
python -m uvicorn app.main:app --reload --port 8000
#   或
python run.py
```

启动后：
- 后端地址：http://127.0.0.1:8000
- 接口文档（Swagger）：http://127.0.0.1:8000/docs
- 健康检查：http://127.0.0.1:8000/

> 说明：后端用**死数据**（内存中 48 条违章记录 + 看板统计），无需数据库。改动审核/删除只在内存生效，重启即恢复初始数据。

---

## 2. 启动前端（终端 2）

```bash
cd D:\University-Competition\Innovation_Entrepreneurship\project\frontend-vue

# 首次运行先装依赖（已装过可跳过）
npm install

# 启动开发服务器
npm run dev
```

启动后浏览器打开：**http://127.0.0.1:5173**

> 前端已配置代理：`/api` 与 `/static` 自动转发到后端 8000 端口，所以**必须先启动后端**再用前端。

### 打包构建（可选，部署用）
```bash
npm run build      # 产物在 dist/
npm run preview    # 本地预览构建产物
```

---

## 3. 登录账号

| 用户名 | 密码 | 角色 |
|--------|----------|--------|
| admin | admin123 | 系统管理员 |
| auditor | 123456 | 安全审核员 |

---

## 4. 页面导航

| 路由 | 页面 | 说明 |
|------|------|------|
| /login | 登录页 | 吊装暮色背景 |
| /dashboard | 首页数据看板 | 统计卡 + 趋势/类别/状态图表 + 最新告警 |
| /records | 监控记录列表 | 多条件筛选 + 表格 + 分页 + 批量删除 |
| /records/:id | 监控详情+审核 | 检测框/电子围栏叠加 + 识别正确/错误/实验正确 |

---

## 5. 常见问题

**Q: 前端页面打不开数据 / 接口 401 或网络错误？**
A: 确认后端已在 8000 端口启动；确认用账号登录拿到 token。

**Q: 端口被占用？**
A: 后端改 `--port 8001`（同时改 `frontend-vue/vite.config.js` 里代理目标）；前端 `npm run dev -- --port 5174`。

**Q: VS Code 里 `import fastapi` 等出现黄色波浪线"无法解析导入"？**
A: 这是编辑器静态检查（basedpyright）**未识别 Python 解释器**所致，**不影响运行**。本工作区以 `project` 为根目录打开，已在 `project/` 根放了 `pyrightconfig.json` 和 `.vscode/settings.json` 指向 Miniconda。让波浪线消失：
  1. 按 `Ctrl+Shift+P` → 输入 `Python: Select Interpreter` → 选 `D:\Miniconda3_python\python.exe`；
  2. 再 `Ctrl+Shift+P` → `Developer: Reload Window` 重载窗口。
  （`backend-python/` 下也留了一份同样的配置，便于单独打开后端目录时也能生效。）

**Q: 想换成独立虚拟环境（不污染全局 conda）？**
A: 在 `backend-python/` 执行：
```bash
python -m venv .venv
.\.venv\Scripts\activate
pip install -r requirements.txt
```
然后把 `pyrightconfig.json` 里的 `extraPaths` 第二项（site-packages 路径）删掉，VS Code 选 `.venv` 解释器即可。

---

## 6. 目录结构速览

```
project/
├─ API-CONTRACT.md        # 前后端接口契约（唯一事实来源）
├─ 启动指南.md            # 本文档
├─ dataset/               # 工作区级数据集（真实吊装/起重机现场图）
│  ├─ images/             # 10 张 Wikimedia 公开授权现场图，挂载到 /static/datasets
│  └─ DATASET-LICENSE.md  # 来源与版权说明
├─ backend-python/        # FastAPI 后端
│  ├─ app/                # 入口/路由/核心/死数据/静态图
│  ├─ requirements.txt
│  └─ run.py
└─ frontend-vue/          # Vue3 前端（项目名 aegis-lift）
   ├─ src/                # 页面/组件/api/路由/状态
   └─ package.json
```
